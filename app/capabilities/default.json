{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Popover window: core events + our own commands.",
  "windows": ["popover"],
  "permissions": ["core:default"]
}
